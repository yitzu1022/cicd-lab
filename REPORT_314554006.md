# CI/CD Lab Report - 314554006

## 實作目標

本作業在 `.github/workflows/ci_314554006.yaml` 新增 GitHub Actions CI pipeline，讓專案在每次 `push` 時自動執行程式品質檢查，包含 TypeScript typecheck、Prettier check 與 Vitest 測試。任一檢查失敗時，整個 pipeline 會顯示失敗。

## Workflow 設計

Workflow 名稱為 `CI 314554006`，觸發條件設定為：

```yaml
on:
  push:
```

因此只要 push 到 GitHub repository 的任一 branch，就會自動啟動 CI。

Job 使用 `ubuntu-latest` 執行，並透過以下步驟完成環境建置：

1. `actions/checkout@v4`：取得 repository 原始碼。
2. `actions/setup-node@v4`：安裝 Node.js 22，符合 `package.json` 中 `engines` 的版本需求。
3. `npm ci`：依照 `package-lock.json` 安裝固定版本的 dependencies，確保 CI 環境可重現。

## 檢查項目

### TypeScript Typecheck

```bash
npm run typecheck
```

此指令執行 `tsc --noEmit`，只進行型別檢查，不輸出編譯檔案。若 TypeScript 型別錯誤，GitHub Actions step 會失敗，pipeline 也會失敗。

### Prettier Check

```bash
npm run format:check
```

此指令執行 `prettier --check .`，確認所有檔案符合專案格式規範。若有檔案格式不符合 Prettier，step 會失敗。

### Test

```bash
npm test -- --reporter=default --reporter=junit --outputFile=test-results/junit.xml
```

此指令使用 Vitest 執行測試。除了在 log 顯示預設測試結果，也額外輸出 JUnit XML 檔案到 `test-results/junit.xml`，供後續 summary 與 artifact 使用。

## 測試結果顯示策略

本作業採用自行實作方式顯示測試結果，不依賴第三方 test reporter action。

`Publish test summary` step 使用 `if: always()`，因此即使前面的測試失敗仍會執行。此 step 會讀取 `test-results/junit.xml`，解析測試總數、通過數、失敗數、錯誤數與 skipped 數量，並寫入 GitHub Actions 的 `$GITHUB_STEP_SUMMARY`。

完成後，測試結果會顯示在 GitHub Actions run 的 Summary 頁面中，方便直接查看測試狀態。

另外 workflow 也使用 `actions/upload-artifact@v4` 上傳 `test-results/junit.xml`，保留原始測試報告供下載與追蹤。

## 失敗處理

GitHub Actions 預設會在 command 回傳非 0 exit code 時標記 step 失敗。因此：

- TypeScript typecheck 失敗時，pipeline 失敗。
- Prettier check 失敗時，pipeline 失敗。
- Vitest 測試失敗時，pipeline 失敗。

`Publish test summary` 與 `Upload JUnit report` 使用 `if: always()` 只負責產生報告，不會掩蓋前面檢查的失敗結果。

## 使用工具

- GitHub Actions：自動化 CI pipeline。
- Node.js 22：專案執行環境。
- npm ci：安裝可重現的 dependencies。
- TypeScript：執行靜態型別檢查。
- Prettier：檢查程式碼格式。
- Vitest：執行單元測試並產生 JUnit XML。
- GitHub Step Summary：在 Actions 結果頁面顯示測試摘要。
- GitHub Actions Artifact：保存 JUnit XML 測試報告。
